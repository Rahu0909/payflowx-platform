apiVersion: v1
kind: Service

metadata:
  name: merchant-service
  namespace: payflowx

spec:
  selector:
    app: merchant-service

  ports:
    - port: 8083
      targetPort: 8083

  type: ClusterIP
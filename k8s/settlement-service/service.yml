apiVersion: v1
kind: Service

metadata:
  name: settlement-service
  namespace: payflowx

spec:
  selector:
    app: settlement-service

  ports:
    - port: 8086
      targetPort: 8086

  type: ClusterIP